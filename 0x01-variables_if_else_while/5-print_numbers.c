#include <stdio.h>

/**
 * main - prints all single digit numbers of base 10 starting from 0,
 * using printf
 *
 * Return: Always 0 (Success)
 */
int main()
{
	int n;
	
	for (n = 0; n < 10; n++)
	{
		printf(n);
	}
	printf('\n');
	return (0);
}
